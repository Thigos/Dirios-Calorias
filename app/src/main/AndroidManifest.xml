<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diriodecalorias">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconenovo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconenovo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiárioDeCalorias">
        <activity
            android:name=".Resultados"
            android:label="@string/title_activity_resultados"
            android:theme="@style/Theme.DiárioDeCalorias.NoActionBar"></activity>
        <activity android:name=".Calculos2" />
        <activity android:name=".Calculos" />
        <activity android:name=".Dicas" />
        <activity android:name=".SobreGrupo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>